<section class="client-body">
    <div class="box">
        Authenticate using the "<strong>Login</strong>" button or end the authentication using the "<strong>Log Out</strong>" button.
    </div>
    <div>
        <div class="level">
            {% if not isAuth %}
            <div class="level-item">
                <a id="authenticate" href="/auth/twitter" class="button is-link">Login With Twitter</a>
            </div>
            {% endif %}
            {% if isAuth %}
                <div class="level-item">
                    <a id="logout" class="button is-link">Log Out</a>
                </div>
            {% endif %}
        </div>
    </div>
</section>
